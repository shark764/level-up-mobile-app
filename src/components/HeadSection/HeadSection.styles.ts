import tailwind from 'tailwind-rn';

const styles = {
  headerSection: tailwind('pt-2 pb-10 flex flex-row justify-between items-center'),
  backSection: tailwind('flex flex-row items-center'),
  backIcon: tailwind('text-gray-500'),
};

export default styles;
