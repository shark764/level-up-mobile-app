import tailwind from '@utils/tailwind';

const styles = {
  container: tailwind('m-3 items-center'),
  icon: [tailwind('m-2'), {width: 95, height: 92}],
  title: tailwind('text-white'),
  body: tailwind('text-off-white')
};

export default styles;
