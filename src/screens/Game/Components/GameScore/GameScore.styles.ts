import tailwind from '@utils/tailwind';

const styles = {
  mainText: tailwind('text-off-white'),
  subText: tailwind('text-gray-medium'),
  scoreText: tailwind('text-white')
};

export default styles;
